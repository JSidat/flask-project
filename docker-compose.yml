version: '3.7'
services:
  flask-app:
    image: flaskproject:latest
    build: .
    ports:
    - target: 5000
      published: 5000
      protocol: tcp
    environment:
      FLASK_PROJECT_DB_URI: ${FLASK_PROJECT_DB_URI}
      SECRET_KEY: ${SECRET_KEY}
  